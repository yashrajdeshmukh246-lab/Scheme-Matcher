# Copy this file to .env and set the backend URL.
# For local development, the default in src/api.js (http://localhost:5000) already works.
# When you host the backend (e.g. on Render/Railway), set it here:
VITE_API_URL=http://localhost:5000
